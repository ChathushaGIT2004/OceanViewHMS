
const BASE_URL = "http://localhost:8080/Ocean-View-System/";



// Default fetch options (can extend later)
const DEFAULT_HEADERS = {
    "Content-Type": "application/json"
};

// Export as CommonJS module
module.exports = {
    BASE_URL,
    ENDPOINTS,
    DEFAULT_HEADERS
};